# 时间裂缝 --- ROADMAP

> 更新日期：2026-09-19\
> 用途：记录开发顺序、未来玩法和支线想法。\
> 当前实际完成状态以 `README.md` 为准。

# 1. 当前最高目标

**用胶囊角色、方块建筑和最简单 UI，让第一关从开始一直运行到胜利 /
失败。**

``` text
开始关卡
→ 据点 + 初始居民
→ 建造资源建筑
→ 分配居民
→ 自动采集 / 搬运
→ 木材 / 石材 / 食物
→ 建造训练建筑
→ 居民训练为战斗职业
→ 敌人
→ 自动战斗
→ Boss
→ 胜利 / 失败
```

# 2. 当前开发章节：FOOD

STONE + Quarry、建造系统第一轮和 Villager UnitPanel 最小版已经完成。
当前进入 FOOD 需求循环开发。

目标：

``` text
工作
→ 工作时长 / 疲劳基础
→ 回据点吃饭
→ 休息
→ 恢复原工作
```

本章节暂时不开发：

-   FOOD
-   疲劳、吃饭、休息
-   Warehouse
-   程序化地图

# 3. 第一关主线待办

当前推荐顺序调整为：

``` text
[已完成] 1. WOOD + LumberCamp
             ↓
[已完成] 2. STONE + Quarry
             ↓
[已完成] 3. 建造系统第一轮
             ↓
[已完成] 4. Villager UnitPanel 最小版
             ↓
[当前] 5. FOOD
          ↓
6. 工作时长 / 疲劳基础
          ↓
7. 居民回据点吃饭
          ↓
8. 休息
          ↓
9. 恢复原工作
          ↓
10. House / 人口上限
          ↓
11. 幸福感
          ↓
12. 训练建筑 / 战斗主线
          ↓
13. 敌人 / Boss
          ↓
14. 胜利 / 失败
```

顺序可以根据实际试玩调整，但不要让支线长期阻塞第一关闭环。

# 4. 资源与物流后续

当前：

``` text
ResourceType
+ ResourceStorage
+ ResourceBase
+ 通用 Villager 携带/运输
```

已经建立。

## Warehouse【后续】

地图扩大后增加通用仓库：

``` text
资源点
→ 附近生产建筑
→ 附近 Warehouse
→ 满仓 / 调度
→ Base
```

Warehouse 目标：

-   可以存储多种资源
-   拥有有限总量或分类容量
-   附近建筑和单位可以就近存取
-   避免所有物流都必须跑回 Base
-   为后续资源管理提供空间布局决策

以后再决定：

-   最近仓库
-   指定仓库
-   资源过滤
-   输入 / 输出优先级
-   搬运工
-   仓库间调拨

当前不实现。

# 5. 居民需求支线

## 工作 / 吃饭 / 休息【第一关需要简化版】

第一版：

``` text
工作 X 秒
→ 回据点
→ 消耗一次食物
→ 休息 Y 秒
→ 继续工作
```

没有食物：

``` text
回据点
→ 等待
→ 有食物
→ 吃饭
→ 休息
→ 工作
```

## 挨饿死亡【后续】

未来可增加：

``` text
STARVATION_DURATION
```

流程：

``` text
没有食物
→ 累计挨饿时间
→ 属性惩罚（可选）
→ 超过极限
→ 死亡
```

当前第一关不做。

# 6. 人口 / 住房 / 幸福感【支线】

## 人口上限

居民楼未来可以同时提供：

``` text
人口上限
+ 居住位
```

人口满后限制继续获得居民。

## 居民来源

暂不锁死，保留：

-   自然增长
-   任务 / 事件救援
-   酒馆 / 难民营等主动招募

不建议简单设计成"支付食物直接凭空购买居民"。

## 住房

未来：

``` text
工作
→ 据点吃饭
→ 回住宅休息
→ 继续工作
```

## 幸福感

未来可加入：

``` text
happiness: 0~100
```

可能来源：

-   是否吃饱
-   是否有住房
-   住宅品质
-   事件 / Buff

可能影响：

-   工作效率
-   采集效率
-   士气

具体公式等第一关试玩后决定。

# 7. Trait 后续任务

当前 Trait 数值系统已经跑通。

未来：

## 随机出生 Trait

暂定：

``` text
第 1 个：100%
第 2 个：50%
第 3 个：10%
```

连续判定；第二个失败后不再判第三个。

## 职业 Trait

训练后从职业池追加 Trait，例如：

``` text
GENERAL
SWORDSMAN
ARCHER
```

## Trait 视觉表现【新增支线】

未来 Trait 可以影响居民体型和视觉：

``` text
Trait
→ UnitVisualModifier
├─ Visual Scale
├─ Collision Scale
├─ 颜色（以后）
├─ 附件（以后）
└─ 特效（以后）
```

例如：

``` text
高大
→ 模型 Scale 增大
→ CharacterBody3D 对应碰撞体同步调整

矮小
→ 模型 Scale 减小
→ 碰撞体同步调整
```

注意：

-   不要简单对整个 `CharacterBody3D` 无脑缩放
-   最好区分视觉节点和物理碰撞节点
-   NavigationAgent3D 半径 / 高度未来也可能需要随体型调整
-   Trait 数据层不要直接依赖具体 Villager 场景节点路径

当前不实现。

# 8. 单位 UI【主线后续】

居民不可直接控制，但可以点击查看：

-   名称
-   职业
-   当前生命
-   移动速度
-   工作速度
-   采集速度
-   当前状态
-   当前工作建筑
-   Trait ICON / 名称 / 等级

后续还可以显示：

``` text
本轮已工作时间
距离休息时间
每餐消耗食物
休息时间
携带资源
```

# 9. 战斗主线

第一关至少完成：

``` text
居民
→ 训练建筑
→ 剑士
→ 寻找敌人
→ 移动到攻击范围
→ 攻击
→ 生命 / 死亡
```

之后：

``` text
Boss 被击败 → 胜利
Base 被摧毁 → 失败
```

第一版 Boss 可以只是数值更强的方块 / 胶囊敌人。

# 10. 全局效果【第一关后】

未来统一方向：

``` text
基础属性
+ Unit Trait
+ 装备
+ 玩家遗物
+ Boss Buff / Debuff
+ 其他 Global Effect
= 最终属性
```

遗物、Boss 效果尽量复用 Modifier 思路，不直接修改单位基础属性。

# 11. 更远期内容

第一关完整可玩后再根据试玩决定是否加入：

-   弓手及更多职业
-   职业升级
-   装备 / 武器 / 技能
-   单位经验等级
-   更多资源和生产链
-   居民关系 / 家庭 / 心情事件
-   疾病 / 受伤 / 医疗
-   昼夜 / 天气 / 季节
-   食物品质
-   不同住宅品质
-   士气 / 阵型
-   更多敌人兵种
-   Boss 多阶段机制
-   随机事件 / 任务
-   遗物
-   科技树
-   多关卡成长 / Meta Progression

# 12. 开发原则

1.  **先闭环，再深度。**
2.  **先胶囊方块，再正式美术。**
3.  新功能先问："它是否帮助第一关从开始运行到结束？"
4.  底层可以预留，玩法不必同时全部实现。
5.  不因为未来可能需要而提前复杂化。
6.  第二种资源应该用于验证通用框架，而不是复制第一种资源代码。
7.  开发一段时间后重新上传完整工程，按实际代码复查 README / ROADMAP。

# 13. 不知道下一步做什么时

> **第一关是否已经能从几个胶囊居民一直玩到击败方块 Boss
> 并出现胜利界面？**

如果不能，继续补这条链。

如果已经可以，再根据实际试玩选择最需要增加深度的系统。

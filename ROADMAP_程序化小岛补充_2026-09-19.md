# 时间裂缝 --- ROADMAP（程序化世界补充）

> 更新日期：2026-09-19
>
> 本补充章节用于并入现有 `ROADMAP_时间裂缝.md`。当前实际工程状态仍以
> README 为准。

# 当前开发章节：STONE + Quarry

当前继续主线：

``` text
Stone ResourceBase
→ Quarry（production_resource_type = STONE）
→ Miner
→ Quarry ResourceStorage
→ 通用 Villager 运输
→ Base ResourceStorage
→ HUD Stone
```

这一章用于验证第二种真实资源能否复用现有通用资源框架，不复制一整套矿工运输状态机。

# 程序化世界方向【已确定】

长期地图采用：

> **Seed 驱动的程序化有限小岛 + 战争迷雾 + 分布在地图各处的资源簇。**

目标是让每次新游戏的世界布局不同，同时通过生成约束保证地图可玩。

``` text
New Game
→ World Seed
→ 生成岛屿轮廓 / 地形
→ 选择合法 Base 出生区域
→ 生成树林 / 石矿 / 食物等资源簇
→ 生成特殊地点 / 敌人
→ 建立 Navigation
→ 战争迷雾覆盖未知区域
→ 玩家探索、生产、扩张
```

## 为什么采用有限小岛

小岛用海岸和海洋自然形成地图边界：

``` text
陆地 → 海岸 → 海洋 → 世界边界
```

这样可以：

-   不需要无限地图；
-   不需要生硬空气墙；
-   控制单局地图规模；
-   控制 Navigation 和生成成本；
-   海岸线本身制造不同的建筑与物流布局；
-   配合战争迷雾，即使地图不巨大也能保留探索未知的感觉。

第一版海洋主要承担自然边界作用，不需要立即制作航海系统。

# 战争迷雾

开局只揭示 Base 周围安全区域，其余岛屿处于未知状态。

``` text
?????????????????
?????? 🌲 ???????
????    🏠    ????
????          ????
?????????????????
```

随着居民活动、建筑扩张或未来侦察机制，逐渐揭示地图：

``` text
Base 周边
→ 扩大活动范围
→ 揭示树林 / 石矿 / 食物
→ 揭示特殊地点
→ 揭示敌人区域
```

长期建议区分：

``` text
未探索
已探索但当前不可见
当前可见
```

第一版只实现满足玩法所需的基础战争迷雾，不提前制作复杂视线高度、遮挡和侦察系统。

# 程序化资源分配

资源不要全地图均匀随机撒点。

优先采用：

``` text
区域概率 / Noise
+
资源簇 Cluster
```

例如：

``` text
树林区                 石矿区
🌲🌲🌲                 🪨🪨
🌲🌲🌲🌲               🪨🪨🪨

           🏠 Base

🪨🪨                    🌲🌲🌲
🪨🪨🪨                  🌲🌲
```

这样资源位置会自然产生建筑与物流决策：

``` text
大片树林 → LumberCamp 位置
石矿集中区 → Quarry 位置
远距离资源 → 未来 Warehouse / 物流布局
```

# 随机必须受到可玩性约束

"每局不同"不等于"完全随机"。

Base 起始安全区至少应保证第一阶段必需条件：

``` text
Starting Safe Area
├─ 足够的 WOOD
├─ 足够的 STONE
├─ 基础 FOOD 来源
├─ 合法建筑空间
├─ 合法 Navigation 区域
└─ 不被海岸 / 障碍完全封死
```

远离 Base 的区域可以拥有更大的随机性。

最终原则：

``` text
强制可玩性约束
+
随机地形与资源布局
=
每局不同但不会生成明显坏档
```

# WorldGenerator 与 ResourceBase 职责分离

未来 `WorldGenerator / ResourceSpawner` 只负责：

``` text
在哪里生成资源
生成什么资源
生成多少资源节点
资源簇大小 / 密度
```

`ResourceBase` 继续只负责：

``` text
resource_type
resource_amount
reserve()
release()
gather()
资源耗尽
```

Tree / Stone 不应该自行决定世界坐标。

Villager、LumberCamp、Quarry
也不应该知道资源来自手工场景还是程序化生成。

因此当前正在制作的 ResourceBase、ResourceStorage、通用 Villager
工作/运输系统可以直接延续到未来程序化世界。

# World Seed

程序化世界预留 Seed：

``` text
World Seed: 834721
```

相同 Seed 应尽量重现：

-   岛屿轮廓；
-   地形；
-   Base 候选出生区域；
-   Tree / Stone / Food 分布；
-   特殊地点；
-   敌人生成信息。

用途包括：

-   重玩同一个世界；
-   分享 Seed；
-   调试生成 Bug；
-   未来挑战 Seed。

# 长期模块划分

建议职责方向：

``` text
WorldGenerator
├─ IslandGenerator
│  └─ 岛屿轮廓 / 高度
├─ SpawnAreaGenerator
│  └─ Base 合法出生区域
├─ ResourceSpawner
│  ├─ Tree Cluster
│  ├─ Stone Cluster
│  └─ Food Cluster
├─ PointOfInterestSpawner
├─ EnemySpawner
├─ FogOfWar
└─ Navigation 更新
```

第一版不要求立刻拆成所有这些脚本，但长期职责边界按此方向设计。

# Warehouse 与程序化地图

地图扩大后，资源距离自然形成物流压力：

``` text
远处资源区
→ LumberCamp / Quarry
→ 附近 Warehouse
→ 区域物流
→ Base
```

Warehouse 后续用于：

-   多资源存储；
-   附近单位就近存取；
-   缩短长距离运输；
-   通过仓库位置形成资源管理和空间布局决策。

以后再考虑资源过滤、输入/输出优先级、搬运工、仓库间调拨。

# 当前不要提前实现

程序化世界方向已经确定，但当前不要让它阻塞胶囊方块第一关闭环。

暂不制作：

-   无限地图；
-   多岛屿航海；
-   河流模拟；
-   真实侵蚀；
-   复杂 Biome；
-   地下 / 洞穴；
-   动态地形破坏；
-   高级道路生成；
-   资源生态模拟；
-   复杂矿脉刷新；
-   高级视线遮挡。

程序化世界第一版目标严格控制为：

> **有限小岛 + Seed + 每局不同 + 合理资源簇 + Base 可玩性保证 +
> 基础战争迷雾。**

# 当前主线顺序

``` text
[当前] STONE + Quarry
↓
建造系统最小版
↓
Base / HUD 最小 Food 数据
↓
Villager 工作 → 吃饭 → 休息
↓
UnitPanel
↓
训练建筑
↓
居民 → 剑士
↓
敌人
↓
自动战斗
↓
Boss
↓
胜利 / 失败
↓
第一关完整试玩
```

程序化世界是明确的长期方向；资源与建造系统稳定后，可以先做一个很小的"随机资源簇测试场"验证
`ResourceSpawner`，但完整岛屿和战争迷雾不要抢占当前 STONE + Quarry
主线。

# Trait 视觉表现支线

已记录：

``` text
Trait
→ UnitVisualModifier
├─ Visual Scale
├─ Collision Shape 同步
├─ NavigationAgent 尺寸（必要时）
├─ 颜色（以后）
├─ 附件（以后）
└─ 特效（以后）
```

例如高大 / 矮小 Trait 改变居民体型，同时同步碰撞体。不要简单无脑缩放整个
CharacterBody3D。

# 新增开发原则

1.  先闭环，再深度。
2.  世界长期采用程序化有限小岛，而不是无限地图。
3.  战争迷雾用来制造探索未知，不依赖巨大地图。
4.  程序化生成必须"随机但可玩"。
5.  资源优先按区域 / 资源簇生成，而不是均匀撒点。
6.  WorldGenerator 决定"资源在哪里"，ResourceBase 决定"资源如何被采"。
7.  Villager / 生产建筑不能依赖资源是手工摆放还是程序化生成。
8.  第二种资源用于验证通用资源架构，不复制 WOOD 专用代码。
9.  Warehouse 等物流深度等地图规模真正产生需求后再实现。
10. 当前继续 STONE + Quarry，不让程序化地图支线阻塞第一关闭环。
